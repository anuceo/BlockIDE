export function IPFSBrowser() {
  return (
    <div style={{ padding: 12 }}>
      <h2 style={{ marginTop: 0 }}>IPFS</h2>
      <div style={{ opacity: 0.8 }}>Stub UI. Next: wire to Tauri `upload_to_ipfs` / `pin_json` commands.</div>
    </div>
  );
}

